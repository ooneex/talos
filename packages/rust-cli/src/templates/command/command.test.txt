import { describe, expect, test } from "bun:test";
import { {{NAME}}Command } from "@module/{{MODULE}}/commands/{{NAME}}Command";

describe("{{NAME}}Command", () => {
  test("should have class name ending with 'Command'", () => {
    expect({{NAME}}Command.name.endsWith("Command")).toBe(true);
  });

  test("should be instantiable", () => {
    const command = new {{NAME}}Command();
    expect(command).toBeInstanceOf({{NAME}}Command);
  });

  test("should have 'getName' method returning a non-empty string", () => {
    const command = new {{NAME}}Command();
    expect(typeof command.getName()).toBe("string");
    expect(command.getName().length).toBeGreaterThan(0);
  });

  test("should have 'getDescription' method returning a non-empty string", () => {
    const command = new {{NAME}}Command();
    expect(typeof command.getDescription()).toBe("string");
    expect(command.getDescription().length).toBeGreaterThan(0);
  });

  test("should have 'run' method returning a Promise", async () => {
    const command = new {{NAME}}Command();
    const result = command.run({});
    expect(result).toBeInstanceOf(Promise);
    await result;
  });
});
