services:
  # MongoDB - NoSQL document database
  # Docs: https://www.mongodb.com/docs/
  # Connection: mongodb://talos:talos@localhost:27017/talos
  mongodb:
    image: mongo:8.0
    container_name: mongodb
    restart: "on-failure"
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db:rw
    environment:
      - MONGO_INITDB_ROOT_USERNAME=talos
      - MONGO_INITDB_ROOT_PASSWORD=talos
      - MONGO_INITDB_DATABASE=talos

volumes:
  mongodb_data:
