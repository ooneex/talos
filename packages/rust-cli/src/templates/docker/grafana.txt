services:
  # Grafana - Metrics visualization and dashboards
  # Docs: https://grafana.com/docs/grafana/latest/
  # Web UI: http://localhost:3000 (talos/talos)
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: "on-failure"
    ports:
      - "4000:3000"
    volumes:
      - grafana_data:/var/lib/grafana:rw
    environment:
      - GF_SECURITY_ADMIN_USER=talos
      - GF_SECURITY_ADMIN_PASSWORD=talos

volumes:
  grafana_data:
