import type { ReactNode } from "react";

// Rendered by the route's `pendingComponent` while the loader stays pending past
// the `pendingMs` threshold. See TanStack Router "Data Loading".
export const {{NAME}}SkeletonLayout = (): ReactNode => {
  return (
    <section aria-busy="true" aria-live="polite">
      <span className="sr-only">Loading…</span>
    </section>
  );
};
