import type { ReactNode } from "react";

// Rendered by the route's `notFoundComponent` when a {{KEBAB}} resource is missing
// (e.g. `throw notFound()` from a loader). See TanStack Router "Not Found Errors".
export const {{NAME}}NotFoundLayout = (): ReactNode => {
  return (
    <section>
      <h1>Not found</h1>
      <p>The {{KEBAB}} you are looking for does not exist.</p>
    </section>
  );
};
