name: rust-cli release

on:
  push:
    tags:
      - "@talos/cli@*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to publish (e.g. @talos/cli@0.1.0)"
        required: true

permissions:
  contents: write

defaults:
  run:
    working-directory: packages/cli

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: talos-darwin-arm64
            os: macos-14
            target: aarch64-apple-darwin
          - name: talos-darwin-x64
            os: macos-13
            target: x86_64-apple-darwin
          - name: talos-linux-x64
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - name: talos-linux-arm64
            os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          - name: talos-windows-x64
            os: windows-latest
            target: x86_64-pc-windows-msvc
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
          components: llvm-tools-preview

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            packages/cli/target
          key: ${{ matrix.target }}-cargo-${{ hashFiles('packages/cli/Cargo.lock') }}

      - name: Sync Cargo.lock package version
        run: cargo update -p talos-cli

      - name: Build release binary
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Package (Unix)
        if: runner.os != 'Windows'
        run: |
          tar -czf "${{ matrix.name }}.tar.gz" -C "target/${{ matrix.target }}/release" talos

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path "target/${{ matrix.target }}/release/talos.exe" -DestinationPath "${{ matrix.name }}.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: |
            packages/cli/${{ matrix.name }}.tar.gz
            packages/cli/${{ matrix.name }}.zip
          if-no-files-found: ignore

  release:
    name: Publish release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag || github.ref_name }}
          files: artifacts/*
          generate_release_notes: true
